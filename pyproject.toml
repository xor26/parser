[project]
name = "parser"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.133.1",
    "sqlalchemy>=2.0.47",
    "uvicorn>=0.41.0",
    "psycopg[binary]",
    "websockets>=16.0",
]
